# Docker Desktop overrides (not used with Podman)
# This file is automatically included by the etest script when running Docker
services:
  worker:
    extra_hosts:
      - "host.docker.internal:host-gateway"
